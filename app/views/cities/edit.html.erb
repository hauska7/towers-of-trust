<section class="text-center">                                                                                                             
  <div class="container">
    <div>
      City Edit |
      <%= link_to "Home", home_path %>
      <%= link_to "Cities", cities_path %>
    </div>

  <form action="/cities" method="PUT">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token -%>
    <% unless @city.errors.empty? %>
      <ul class="list-group">
        <% @city.errors.full_messages.each do |message| %>
          <li class="list-group-item">
            <%= "#{message}" %>
          </li>
        <% end %>
      </ul>
    <% end %>
    
    <div class="form-group">
      <label for="name">Name</label>
      <input type="text" class="form-control" id="name"><%= @city.name %></input>
    </div>
    <div class="form-group">
      <label for="state">State</label>
      <input type="text" class="form-control" id="state"><%= @city.state %></input>
    </div>
    <div class="form-group">
      <label for="country">Country</label>
      <input type="text" class="form-control" id="country"><%= @city.country %>
    </div>
    <button type="submit" class="btn btn-primary">Update</button>
  </form>
  </div>
</section>
